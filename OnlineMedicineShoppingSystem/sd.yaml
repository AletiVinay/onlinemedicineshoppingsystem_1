---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: onlinemedicineshoppingsystem-prod
  labels:
       app: myapp
spec:
  replicas: 1
  template:
    metadata:
      name: onlinemedicineshoppingsystem-pod
      labels:
        app: myapp
    spec:
      containers:
        - name: onlinemedicineshoppingsystem
          image: vinay265/onlinemedicineshoppingsystem
  selector:
    matchLabels:
      app: myapp

---
apiVersion: v1
kind: Service
metadata:
  name: appservice
spec:
  type : NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30001
  selector:
    app: myapp

